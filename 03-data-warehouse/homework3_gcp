CREATE OR REPLACE EXTERNAL TABLE `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_external`
OPTIONS (
  format = 'PARQUET',
  uris = [
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-01.parquet',
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-02.parquet',
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-03.parquet',
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-04.parquet',
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-05.parquet',
    'gs://zoomcamp_hmw3/yellow_tripdata_2024-06.parquet'
  ]
);

SELECT * FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_external` limit 10;


-- Create a non partitioned table from external table
CREATE OR REPLACE TABLE turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned AS
SELECT * FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_external`;

--Question 1. Counting records
--What is count of records for the 2024 Yellow Taxi Data?
--20,332,093
SELECT count(*)  
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned`;

--Question 2. Data read estimation
--Write a query to count the distinct number of PULocationIDs for the entire dataset on both the tables.
--What is the estimated amount of data that will be read when this query is executed on the External Table and the Table?

--Materialized table 155.12 MB
SELECT count(distinct PULocationID)  
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned`;

--External table 0 MB
SELECT count(distinct PULocationID)  
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_external`;


--Write a query to retrieve the PULocationID from the table (not the external table) in BigQuery. Now write a query to retrieve the PULocationID and DOLocationID on the same table.
--Why are the estimated number of Bytes different?
--155.12 MB
--310.24 MB
SELECT PULocationID,DOLocationID 
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned`;

--8,333
SELECT count(*)
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned`
where fare_amount=0;

-- Creating a partition and cluster table
--Question 5. What is the best strategy to make an optimized table in Big Query if your query will always filter based on tpep_dropoff_datetime and order the results by VendorID (Create a new table with this strategy) 
CREATE OR REPLACE TABLE `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_partitioned_clustered`
PARTITION BY DATE(tpep_dropoff_datetime)
CLUSTER BY VendorID AS
SELECT * FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned`;

--Question 6. Write a query to retrieve the distinct VendorIDs between tpep_dropoff_datetime 2024-03-01 and 2024-03-15 (inclusive). Use the materialized table you created earlier in your from clause and note the estimated bytes. Now change the table in the from clause to the partitioned table you created for question 5 and note the estimated bytes processed. What are these values?

SELECT DISTINCT VendorID 
FROM turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_non_partitioned
WHERE DATE(tpep_dropoff_datetime) BETWEEN '2024-03-01' AND '2024-03-15';

SELECT DISTINCT VendorID 
FROM `turing-thought-485711-v0.ny_taxi_hmw3.yellow_tripdata_partitioned_clustered`
WHERE DATE(tpep_dropoff_datetime) BETWEEN '2024-03-01' AND '2024-03-15';

